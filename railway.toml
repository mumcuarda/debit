[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn api.server:app --host 0.0.0.0 --port {PORT}

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r api/requirements.txt"]
