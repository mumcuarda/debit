[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn api.server:app --host 0.0.0.0 --port ${PORT}"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[variables]
NIXPACKS_PYTHON_REQUIREMENTS_PATH = "api/requirements.txt"
